// Package model holds data types and constants used by the output service.
// It is intentionally free of behaviour so it can be imported from anywhere
// without pulling in business logic.
package model

// DefaultPrefix is the CLI binary name used when no prefix is configured.
const DefaultPrefix = "tddmaster"
